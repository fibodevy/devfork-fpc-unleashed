name: CI

on:
  workflow_dispatch:
  
jobs:
  windows:
    uses: ./.github/workflows/windows.yml
    permissions:
      contents: write
      
  linux:
    uses: ./.github/workflows/linux.yml
    permissions:
      contents: write
      
